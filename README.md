# Netball Coach App — Reports.tsx

This repository contains the full Reports.tsx source for a React Native Netball Coach App.
It includes CSV export, HTML export, dynamic stat columns, team stats, player stats,
and match statistics logic.

This file is intentionally public for AI analysis.
